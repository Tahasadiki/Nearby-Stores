server:
  port: 9000

spring:
  application:
    name: eurekaServer


eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone:  http://localhost:9000/eureka

  instance:
    hostname: localhost
